{
  "name": "alpha-token",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ saroznep/alpha-token",
    "create-local": "graph create --node http://localhost:8020/ saroznep/alpha-token",
    "remove-local": "graph remove --node http://localhost:8020/ saroznep/alpha-token",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 saroznep/alpha-token"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
